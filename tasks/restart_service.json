{
  "supports_noop": false,
  "description": "Stop or restart a service or list of services on a node.",
  "parameters": {
    "service": {
      "description": "The name of the service, or a list of service names to stop.",
      "type": "String[1]"
    }
  },
  "implementations": [
    { "name": "restart_service.sh", "requirements": ["shell"] },
    { "name": "restart_service.ps1", "requirements": ["powershell"] }
  ]
}
